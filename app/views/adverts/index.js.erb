$("#adverts").html("<%= escape_javascript(render("masonry")) %>");

// INITIALIZE MASONRY AGAIN TO RELOAD ITEMS
// THIS IS NEEDED BECAUSE OF THE AJAX REQUEST
var $container = $('#masonry-container');
	// initialize
$container.masonry({
	// options 
  	itemSelector: '.box',
	isFitWidth: true	 
});
	// reload of the items
$container.masonry('reloadItems');
	